# 项目工作日志

最后更新：2026-09-22

## 项目概述

这是一个轻量原生 Android 阅读书源整理工具，包名为 `com.mina.yuedu`。主要用途是导入本地或网络书源，进行合并、去重、清理、可用性校验、健康追踪、分类导出和导入阅读。

项目基于 Mina-kk/yuedu-source-dedupe 二次开发，当前仓库：

```text
https://github.com/MIXUULS/yuedu-source-dedupe
```

本地路径：

```text
C:\Users\liuss\Desktop\book\yuedu-source-dedupe-main
```

## 当前版本

- 版本：`v3.7.0`
- versionCode：`310`
- applicationId：`com.mina.yuedu`
- minSdk：24（Android 7.0+）
- targetSdk / compileSdk：35
- JDK：17
- Gradle：8.7
- 当前分支：`main`
- 远程：`origin/main`
- 最新推送提交：`218a5b3 docs: rename release notes to v3.7.0`
- 远程标签：`v3.7.0`

GitHub 上的 `v3.6.0` 是旧正式 Release；本次新改动使用 `v3.7.0`，不要覆盖旧 Release。

## 功能模块

- 去重：标准、严格、激进、名称相似度四种模式。
- 导入：本地 JSON、网络 JSON、YCK 地址、剪贴板书源。
- 校验：搜索、发现、详情、目录、正文五步校验，支持快速模式。
- 网络：直连、系统代理回退、GZIP、重试、同域串行下载。
- 结果：统计、搜索过滤、明细、多选、分类导出、CSV、同域合并。
- 健康：历史校验、质量评分、健康看板、报告、同域优化建议。
- 管理：设置备份/还原、URL 历史、版本对比、变更报告、去重预设。
- 分享：复制文本、保存 JSON、二维码、分享链接。
- YCK：WebView 浏览、站点切换、自动收集书源链接。

## 主要代码位置

- `MainActivity.java`：主界面、导入导出、校验入口、状态和生命周期。
- `core/DedupeEngine.java`：URL/名称去重和保留源评分。
- `core/NameSimilarity.java`：名称归一化和相似度分组。
- `core/MiniJson.java`：轻量 JSON 解析和序列化。
- `network/FetchManager.java`：网络书源下载、并发队列、解析队列。
- `network/SourceParser.java`：书源 JSON/YCK 内容解析。
- `check/CheckSourceEngine.java`：五阶段书源可用性校验。
- `check/HttpProbe.java`：校验请求、重定向、GZIP、代理和状态码。
- `check/AnalyzeUrlLite.java`：legado URL、POST、请求头选项解析。
- `core/SourceHealthTracker.java`：健康数据和质量评分。
- `res/layout/page_dedupe.xml`：主界面布局。
- `res/layout/dialog_check_source.xml`：校验设置弹窗。

## 已完成改动

### 稳定性修复

已完成 2026-09-04 修复报告中的 17 项修复，重点包括：

- 修复大书源导入时下载 worker 因并发槽位判断错误而提前退出。
- 修复 URL 去重模式丢失 invalid 列表。
- 修复校验结果过滤列表使用旧数据。
- 防止重复启动校验引擎。
- 修复校验历史菜单索引错位。
- 修复导出分组日期标签被覆盖。
- 改进 legado URL 选项切分、method 判定和嵌套 headers。
- 限制 MiniJson 递归深度，避免异常 JSON 闪退。
- 支持 HTTP 到 HTTPS 重定向和无错误体状态码。
- 修复全角空格导致的名称去重遗漏。
- 修复线程池泄漏、WebView 泄漏和回调竞态。
- 修复新近度评分长期恒定的问题。

### UI 和交互

- 首页改为蓝绿色明暗主题。
- 去重统计改为 2×2 卡片布局。
- 运行卡片中的停止按钮改为紧凑布局。
- 校验设置增加检测步骤、过滤条件、请求参数和书源类型分区。
- 修复首页内容被 Tab 栏遮挡。
- “全部放弃”解析会清除当前网络批次，不残留已加载数据。

## 测试与构建

已验证：

```text
Gradle 8.7
:app:testDebugUnitTest        通过
:app:compileDebugJavaWithJavac 通过
:app:assembleDebug            通过
:app:assembleRelease          通过
git diff --check               通过
```

真机：

- 设备：OPPO PJD110
- ADB serial：`64367acb`
- Debug v3.7.0：已安装并启动。
- 首页截图检查通过。
- 未发现 `FATAL EXCEPTION`。

构建产物：

```text
app/build/outputs/apk/debug/app-debug.apk
app/build/outputs/apk/release/app-release.apk
```

当前 Release APK 约 2.19 MB。最近构建的 APK SHA-256：

```text
174567962DFA710EE9C7CFC0433C02646F97DC921EFD2E161B88A6E4A4B5C278
```

## GitHub 发布状态

- `origin/main` 已推送到 `218a5b3`。
- 远程标签 `v3.7.0` 指向 `218a5b3`。
- GitHub 已存在旧的 v3.6.0 Release。
- v3.7.0 Release 由用户在 GitHub 页面创建并上传 `app-release.apk`。
- GitHub API 检查曾因未认证请求触发 403 rate limit；以 GitHub Release 页面和远程标签为准。

## 当前工作区注意事项

以下文件是本地测试产生的临时文件，不应提交：

```text
.gradle-home/
device-smoke.png
device-smoke-top-fixed.png
```

它们已加入 `.gitignore`。

## 文档

- `README.md`：项目说明、功能、构建和版本日志。
- `修复报告-2026-09-04.md`：17 项 Bug 修复的详细报告。
- `发布准备记录-2026-09-22.md`：v3.7.0 发布准备过程。
- `发布文案-v3.7.0.md`：GitHub Release 和群公告文案。
- `AGENTS.md`：交接给其他智能体的快速说明。

## 已知限制

- 校验引擎尚未完整支持书源 `charset`，GBK 老站可能误判。
- POST body 中 `{{key}}` 仍使用 URL 编码关键词。
- 多段 `|` URL 尚未逐段轮换探测。
- Release APK 上传和 GitHub Release 页面内容需要以用户网页操作结果为准。

## 后续建议

1. 继续观察 v3.7.0 用户反馈。
2. 后续增加 charset 自动识别或可选编码。
3. 增加单个书源快速重验。
4. 增加解析前 URL 数量/重复预览。
5. 真机出现问题时优先抓取 `adb logcat` 和复现步骤。
