# 发布准备记录（2026-09-22）

## 当前状态

- 项目：阅读书源去重
- 当前配置版本：v3.6.0
- 当前 versionCode：310
- Git 状态：存在未提交改动
- 发布判断：v3.6.0 Debug 真机确认和 Release 构建均已完成，可进入发布整理

## 已完成改动

### Bug 修复

已完成并保留 2026-09-04 修复报告中的 17 项修复，主要涉及：

- 大书源网络解析并发队列
- URL 去重无效源统计
- 校验结果搜索过滤
- 重复校验启动保护
- 校验历史菜单索引
- 导出分组日期标签
- legado URL、POST 和嵌套请求头解析
- MiniJson 深层嵌套保护
- HTTP 重定向和空错误响应处理
- 名称全角空格归一化
- 线程池、WebView 和回调竞态
- 新近度评分

### UI 调整

- 主色调整为蓝绿色，降低单一紫色占比。
- 明暗主题同步调整。
- 去重统计卡片改为 2×2 布局。
- 卡片圆角统一为 8dp。
- 运行状态卡片中的停止按钮改为紧凑布局。
- 校验设置弹窗增加“检测步骤、过滤条件、请求参数、书源类型”分区。
- 修复首页顶部内容被 Tab 栏遮挡的问题。

### 功能修复

- “全部放弃”解析时清除当前网络批次。
- 防止解析队列中已完成结果在放弃后继续写入。
- 放弃后重新计算本地书源结果，避免旧网络数据残留。

## 验证结果

### 自动验证

- Gradle 版本：8.7
- JDK：17.0.20
- 单元测试：通过
- Debug Java 编译：通过
- Release 构建：通过
- `git diff --check`：通过

### 真机验证

- 设备：OPPO PJD110
- ADB 状态：device
- v3.6.0 Debug APK：已安装并启动
- versionCode：310
- versionName：3.6.0
- 主界面：正常显示
- 首页顶部遮挡：已修复
- 当前进程：正常运行
- `FATAL EXCEPTION`：未发现

## 构建产物

### Debug

```text
app/build/outputs/apk/debug/app-debug.apk
```

大小约 6.5 MB。

### Release

```text
app/build/outputs/apk/release/app-release.apk
```

大小约 2.19 MB。

v3.6.0 Release 已重新构建。

签名校验：

- APK v2：通过
- 签名者数量：1

## 发布前待办

1. 清理不应进入 Git 的临时文件：

```text
.gradle-home/
device-smoke.png
device-smoke-top-fixed.png
```

2. 检查 Git diff 后再决定是否提交。
3. 正式发布前保留 release.keystore 备份。

## 已知限制

- 校验引擎仍未完整支持书源 `charset`，GBK 老站可能误判。
- POST body 中 `{{key}}` 仍使用 URL 编码关键词。
- 多段 `|` URL 尚未逐段轮换探测。
- 当前 v3.6.0 Release APK 已完成构建，正式发布前仍需确认待提交文件和发布附件。
