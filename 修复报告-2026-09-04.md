# 修复报告：17 项 Bug 修复（2026-09-04）

> 本文档供其他 AI / 人工复查使用。列出本次全部改动的问题背景、根因、修复方案与验证方式。
> 项目：yuedu-source-dedupe（阅读书源去重）· 基线版本 v3.6.0（versionCode 309）
> 改动均为 Java 源码级修复，未改资源文件、未改构建配置、未提交 git。

## 修复总览

| # | 严重度 | 文件 | 问题一句话 |
|---|--------|------|-----------|
| 1 | 高 | `network/FetchManager.java` | worker 把"并发已满"当"队列取完"提前退出，导入静默丢数据 |
| 2 | 高 | `core/DedupeEngine.java` | URL 模式去重丢弃 invalid 列表，无效源凭空消失 |
| 3 | 高 | `MainActivity.java` | 校验结果搜索过滤列表永不失效，导出/明细操作旧数据 |
| 4 | 高 | `MainActivity.java` | `runCheck` 无运行守卫，可并发启动第二个校验引擎 |
| 5 | 中 | `MainActivity.java` | 校验历史菜单按硬编码下标分发，"重验"项缺席时索引错位 |
| 6 | 中 | `MainActivity.java` | 导出时第二处 `put` 覆盖刚写入的 `✔日期` 分组标记 |
| 7 | 中 | `check/AnalyzeUrlLite.java` | 选项切分/方法判定/嵌套 headers 三处解析缺陷 |
| 8 | 中 | `core/MiniJson.java` | 无递归深度上限，深层嵌套 JSON 抛 StackOverflowError 闪退 |
| 9 | 中 | `check/HttpProbe.java` | 不跟随 http→https 重定向；4xx/5xx 空 body 抛异常绕过自定义状态码 |
| 10 | 中 | `core/NameCleaner.java`、`core/NameSimilarity.java` | 全角空格(U+3000)不参与归一化，NAME 模式漏合并 |
| 11 | 低 | `check/CheckSourceEngine.java` | 空目标提前 return 只关 `pool` 不关 `doCheckPool`，线程池泄漏 |
| 12 | 低 | `MainActivity.java` | exportCsv 对共享结果列表原地排序 |
| 13 | 低 | `MainActivity.java` | 历史速度菜单展示当前结果却标成"该次"数据 |
| 14 | 低 | `MainActivity.java` | 还原备份后并发数未按滑条上限钳制，打开校验弹窗崩溃 |
| 15 | 低 | `MainActivity.java` | `clearAll` 的 gen 检查在锁外，清空瞬间旧回调可"复活"数据 |
| 16 | 低 | `MainActivity.java` | WebView 从不 destroy()，JS 桥持有 Activity 造成泄漏 |
| 17 | 低 | `core/DedupeEngine.java`、`core/NameSimilarity.java` | lastUpdateTime 除以 1e9 恒饱和，新近度评分失效 |

---

## 详细说明

### 1. FetchManager worker 提前退出导致导入丢数据（高）

- **现象**：导入大书源包时只导入第一批，界面却显示"解析完成"；下载 URL 数 > 实际解析数。
- **根因**：`QueueState.takeNext()` 对"`inFlight>=limit`（并发已满）"和"队列已取空"两种情况都返回 `null`，worker 循环把 `null` 一律当"取空"而 `break`。而 `inFlight` 的递减发生在**串行解析队列**的 `complete()` 里——只要解析比下载慢（大包必然如此，串行解析队列正是为此设计），所有 worker 会在第一批下载后全部退出，`domainBuckets` 里剩余 URL 永远不会被取走。这是 v3.1.0 引入串行解析队列时的回归。
- **修复**：新增 `QueueState.downloadDone()` 只递减 `inFlight`（释放下载槽位）；`complete()` 改为纯统计（completed/succeeded/failed/sources），不再动 `inFlight`。worker 在成功提交解析任务后、以及下载失败 catch 中调用 `downloadDone()`。此后 worker 调用 `takeNext()` 时自身槽位必然已释放，`inFlight>=limit` 永不为真，`null` 只可能意味着"队列真取空"。
- **语义保持**：`isDone()`、进度上报中的 `completed` 统计不变；`inFlight` 现在准确表示"正在下载数"。

### 2. URL 模式去重丢弃 invalid 列表（高）

- **现象**：标准/严格/激进模式下，缺 URL/空 URL/非法 URL 的书源从统计中消失（"错误"卡片不计、不出现在任何列表），原始数 ≠ 保留+重复+无效。NAME 模式正常。
- **根因**：`DedupeEngine.run()` 末尾调用 4 参 `buildResult` 重载，其内部 `new ArrayList<InvalidSource>()` 新建空列表，把已收集的 `invalid` 丢弃。
- **修复**：改为调用 5 参重载直接传入 `invalid`；删除 4 参重载防止回归。

### 3. 校验结果过滤列表永不失效（高）

- **现象**：校验后输入搜索关键词，再重新校验/清空/批量移除后，明细弹窗、CSV 导出、分类导出、合并同域操作的仍是旧一轮的列表，与顶部统计卡片显示的新数据不一致。
- **根因**：`filteredCheckResults` 只在搜索框 TextWatcher 里赋值；所有 `checkResults` 的替换/清空路径都不重置它。
- **修复**：新增 `refreshCheckSearchFilter()`（按搜索框当前文本重新应用过滤），在 5 个变更点调用：`startParse`、`clearAll`、`runCheck` 开头、`runCheck` 的 `onFinished`、`removeSourcesFromResult`。

### 4. 运行中可重复发起校验（高）

- **现象**：解析/校验进行中时，右上角菜单的"增量重验/重验失败源"仍可点击，会并发启动第二个 `CheckSourceEngine`：覆盖 `checkEngine` 导致旧引擎无法取消、fetch/check 进度互相覆盖 UI、旧引擎 `onFinished` 落地时把新一轮的运行状态全部清掉。
- **修复**：`runCheck` 顶部加守卫：`fetchRunning || checkRunning` 时 toast 提示并 return。

### 5. 校验历史菜单索引错位（中）

- **现象**：某次校验全部成功（`badUrls` 为空）时，菜单少一项"重验"，但点击处理仍按固定下标：点"查看最快/最慢"会去重验空列表（提示误导），点"取消"反而打开快源排行。
- **修复**：构建菜单项时同步记录各项实际下标（`idxDetail/idxRecheck/idxSpeed`），点击按记录下标分发；"取消"无需处理。

### 6. 导出时 ✔日期分组标记被覆盖（中）

- **现象**：导出的书源大多数丢失 `✔2026-9-4小说（120）` 日期标记；仅当原分组恰好已含精确分类词时保留。该标记是跨次导出防堆叠的既定功能。
- **根因**：`exportPayload` 先 `put("bookSourceGroup", 原分组+标记)`，随后"自动分类标签"块基于**不含标记**的旧分组串重建 parts 并二次 `put`，覆盖第一次的结果。
- **修复**：分组串只组装一次、只写一次：原分组 parts + 分类标签（若 parts 中无）+ `✔日期标记`，统一 join 后单次 `put`。顺带修复空分组时 `"".split(",")` 产生的 `[""]` 导致输出前导逗号的问题。

### 7. AnalyzeUrlLite 三处解析缺陷（中）

影响校验引擎对 legado 搜索/发现 URL 的理解，都会造成**误判"搜索失效/发现失效"**：

- **7a 选项切分**：旧实现 `lastIndexOf(",{")` 从右往左找选项起点，body 值内嵌套的 `},{`（如 `{"list":[{"a":1},{"b":2}]}`）或多段 URL 时切错位置，整段选项 JSON 被拼进 URL 发请求。修复：新增 `openingBraceOfTrailingObject()`，正向做字符串/转义感知的花括号配对，返回与末尾 `}` 配对的顶层 `{`；要求其前一字符是 `,`、片段平衡且延伸到字符串结尾、含 method/body/header 提示键才切分。
- **7b method 判定**：旧实现 `lower.contains("method") && lower.contains("post")` 对整个选项串做子串匹配，body/URL 里出现 "post" 一词就把 GET 误翻成 POST。修复：用 `extractJsonString(opt,"method")` 读键值；有 body 且未声明 method 时按 legado 惯例视为 POST。
- **7c 嵌套 headers**：旧实现只读顶层字符串键 `User-Agent/Cookie/Referer`，legado 标准的嵌套 `"headers":{...}` 对象被静默丢弃，需要 Referer/Cookie 的源被误判。修复：新增 `extractJsonObject()`（平衡花括号提取），从嵌套对象中补读请求头，顶层键兼容保留。

### 8. MiniJson 无递归深度上限（中）

- **现象**：网络源返回深层嵌套 JSON（如 `[[[[…` 数万层）时抛 `StackOverflowError`。它是 `Error` 不是 `Exception`，绕过所有入口的 `catch(Exception)`（`SourceParser.parseArray`、MainActivity 各解析点）直接闪退。
- **修复**：加 `depth` 计数，`object()/array()` 进入时 `enter()` 检查，上限 256，超限抛 `IllegalArgumentException`（各调用方已有 catch，会转为"非书源数组"等友好失败）。`number()` 对空串/坏数字改为带位置的 `IllegalArgumentException`，不再抛裸 `NumberFormatException("empty String")`。

### 9. HttpProbe 两处（中）

- **9a 跨协议重定向**：JDK 的 HttpURLConnection 不跟随 http→https 重定向，只返回 301 短页；导入路径（FetchManager）有手动跟随循环而校验路径没有，导致 `http://` 源被误判"搜索失效"。修复：`doFetch` 改为循环结构，见到 3xx 带 Location 手动跳转（≤5 跳，与 FetchManager 一致），每跳重新套用 method/headers/body。
- **9b 空 body 错误码**：4xx/5xx 且服务器未返回错误体时，`getErrorStream()` 为 null，旧代码回退 `getInputStream()` 直接抛 IOException，`Response` 不会构建，自定义可用状态码白名单（如手动放行 403/500）失效。修复：两路流都为 null 且 code≥400 时直接返回 `Response(code, "", url, …)`，交给调用方按状态码判定。

### 10. 全角空格破坏 NAME 模式去重（中）

- **现象**：`"笔趣阁　官方"`（内含 U+3000）归一化后仍带尾随 U+3000，与 `"笔趣阁"` 判为不同名称，同站镜像源漏合并。中文标题里全角空格极常见。
- **根因**：Java 正则 `\s` 只匹配 `[ \t\n\x0B\f\r]`，`String.trim()` 只剥 ≤U+0020 的字符，两者都不处理 U+3000/U+00A0/U+2007/U+202F。
- **修复**：`NameCleaner.clean` 与 `NameSimilarity.normalize` 的正则改为 `[\\s\\u3000\\u00A0\\u2007\\u202F]+`。未用 `String.strip()`（需高版本 API，项目 minSdk 对应 Android 7.0）。

### 11. 空目标时 doCheckPool 泄漏（低）

`CheckSourceEngine.checkAll` 在 `total==0` 提前 return 时只 `pool.shutdownNow()`，`doCheckPool`（非守护线程）空转泄漏；每执行一次"全部源被过滤光"的校验泄漏一个 N 线程池。修复：两池同关。

### 12. exportCsv 原地排序共享列表（低）

`getCheckResultsForDisplay()` 返回的是共享列表引用，CSV 导出直接 `list.sort(...)` 会永久改变明细/导出的展示顺序（其他消费方都是先拷贝）。修复：`new ArrayList<>(...)` 后再排序。

### 13. 历史速度菜单文案失实（低）

`showHistorySpeed(e)` 的参数 `e` 从未使用（历史记录未保存逐源耗时，无法回放某一次），展示的是当前 `checkResults` 却在菜单里标为"该次可用源最快/最慢"。修复：文案改为"查看当前结果最快/最慢"，弹窗标题改为"当前结果快源排序"，并加注释说明。保留参数以备未来把逐源耗时存入历史。

### 14. 还原备份后滑条越界崩溃（低）

备份 JSON 的 `checkConcurrency` 未按 UI 滑条上限（100）钳制（`normalize()` 只钳到 MAX_CONCURRENCY=500），还原手改备份后 `Slider.setValue` 抛 `IllegalStateException` 崩溃。修复：`bindCheckDialog` 对超时/并发两个滑条都按 `getValueFrom()/getValueTo()` 钳制后再 setValue。

### 15. clearAll 与在途回调的竞态（低）

`onItem/onFailure` 的 `gen != fetchGen` 检查在 `synchronized` 块**外**；`clearAll` 自增 gen 与清空 buckets 不是原子的，已通过检查的旧回调可能在清空完成后进入锁，把刚删除的数据"复活"。修复：gen 检查移入对应锁内（onItem→buckets 锁，onFailure→extraInvalid 锁）。

### 16. WebView 泄漏（低）

`onDestroy` 从不销毁 `yck`（WebView），而 JS 桥 `YckBridge` 持有 Activity 方法引用、`YckWebClient` 回调可越过 teardown 继续操作它。修复：`onDestroy` 中 stopLoading → 从父容器移除 → `setWebViewClient(null)` → `destroy()`；Client 各回调与 `injectYckCollector` 加 `destroyed` 守卫。

### 17. 新近度评分失效（低）

`lastUpdateTime` 是毫秒时间戳，旧公式 `min(50, ms/1e9)` 对 1970 年后的任何时间戳都恒为 +50，只能区分"有无时间戳"。修复：改为 `50 - min(50, 天数/30)`（最近更新 50 分起，每 30 天衰减 1 分，下限 0），提取为 `DedupeEngine.recencyBonus()`；`NameSimilarity.score` 中重复的同款公式改为复用它。

---

## 测试与验证

**新增/修改的测试**（`app/src/test/java/com/mina/yuedu/`）：

| 文件 | 用例 | 覆盖 |
|------|------|------|
| `core/DedupeEngineTest.java`（新） | 3 个 | #2 无效源计数守恒；URL 重复分组不回归；#17 新近度有区分度 |
| `check/AnalyzeUrlLiteTest.java`（新） | 7 个 | #7a 嵌套花括号切分；#7b method 键判定/body 默认 POST；#7c 嵌套 headers；顶层键兼容；`,{` 非选项不切分 |
| `core/MiniJsonTest.java`（新） | 6 个 | #8 深嵌套抛 IAE 而非 SOE；256 层内正常；尾逗号/坏数字报错可读；基本类型；stringify 往返 |
| `core/NameSimilarityTest.java`（增） | +1 | #10 全角空格归一化 |

**结果**：`gradlew :app:testDebugUnitTest :app:compileDebugJavaWithJavac` → **77 tests, 0 failures, 0 errors, 0 skipped**，debug 编译通过（仅存在改动前就有的 deprecation/unchecked 警告）。

**验证修复 #1 的建议**（无法单测网络栈，需真机/人工）：导入一个含数百 URL 的大书源包（并发 ≥8），确认"已发现"条数 ≈ 输入 URL 覆盖的源数、`已下载/总数` 走满，而不是停在第一批。

## 未修复的已知局限（功能缺口，非本次 bug）

1. 校验引擎不支持书源 `charset`（如 GBK）声明：关键词按 UTF-8 编码、响应按 UTF-8 解码，GBK 老站会误判。属新功能。
2. POST body 中 `{{key}}` 替换为 URL 编码后的关键词（既有行为，保持不变；legado 本体 body 用原文，可后续单独讨论）。
3. 多段 `|` URL 只做选项剥离，不逐段轮换探测。

## 改动文件清单

- `app/src/main/java/com/mina/yuedu/MainActivity.java`
- `app/src/main/java/com/mina/yuedu/network/FetchManager.java`
- `app/src/main/java/com/mina/yuedu/core/DedupeEngine.java`
- `app/src/main/java/com/mina/yuedu/core/NameCleaner.java`
- `app/src/main/java/com/mina/yuedu/core/NameSimilarity.java`
- `app/src/main/java/com/mina/yuedu/core/MiniJson.java`
- `app/src/main/java/com/mina/yuedu/check/AnalyzeUrlLite.java`
- `app/src/main/java/com/mina/yuedu/check/HttpProbe.java`
- `app/src/main/java/com/mina/yuedu/check/CheckSourceEngine.java`
- `app/src/test/java/com/mina/yuedu/core/DedupeEngineTest.java`（新）
- `app/src/test/java/com/mina/yuedu/core/MiniJsonTest.java`（新）
- `app/src/test/java/com/mina/yuedu/check/AnalyzeUrlLiteTest.java`（新）
- `app/src/test/java/com/mina/yuedu/core/NameSimilarityTest.java`（增 1 用例）
